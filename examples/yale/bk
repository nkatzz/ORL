% Event Calculus domain-independent axioms.
holdsAt(F,T2) :- initiatedAt(F,T1), fluent(F), next(T1,T2).
holdsAt(F,T2) :- holdsAt(F,T1), fluent(F), not terminatedAt(F,T1), next(T1,T2).
holdsAt(F,0) :- initially(F).

:- initiatedAt(F,T), terminatedAt(F,T).

fluent(loaded). fluent(dead). fluent(alive).
event(load). event(shoot).

initially(alive).

time(0..11).


